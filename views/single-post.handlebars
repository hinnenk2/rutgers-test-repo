<!--

<article class="post">
  <div class="title">
    <a href="{{post.post_url}}" target="_blank">{{post.title}}</a>
    <span>({{post.post_url}})</span>
  </div>
  <div class="meta">
    {{post.vote_count}} point(s) by {{post.user.username}} on
    {{post.created_at}}
    |
    <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
  </div>
</article>
-->

{{> post-info post }}

{{#if loggedIn}}
<form class="comment-form">
  <div>
    <textarea name="comment-body"></textarea>
  </div>

  <div>
    <button type="submit">Add comment!</button>

  </div>
</form>
{{/if}}

<!-- this is array so we can deconstruct; this. is required
 Because arrays don't have named properties like objects do, we're forced to access the array as
 -->

{{> comments post.comments}}

<!-- replaced with partial comments.handlebars
<div class="comments">
  {{#each post.comments}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>
-->

<!-- Do not load if not looged in -->
{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}

